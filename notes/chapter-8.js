// Variable Names
let number_of_eggs = 8

let day_of_the_month = 12

// Function Names 

function fetchUserData() {

}

function loginUser() {
    // step 1 - access the user email and password

    // step 2 - authenticate the user email and password and fetch respond

    // step 3 - redirect the user to the home page if login was successful, and otherwise display an error
}

//! bad example
// function loginUserAndFetchData() {
    
// }

// Comment

// Avoid all convoluted statements
const boolean_check = 14 > 7 ? 21 < 18 ? 'hi mom' : 'bye mom' : 'banana not muffin'
